# Быстрый старт 🚀

## Что нужно сделать перед запуском:

### 1. Установить MongoDB

**Вариант А: Локально**
- Скачайте: https://www.mongodb.com/try/download/community
- Установите и запустите сервис MongoDB

**Вариант Б: Облако (проще для начала)**
- Зайдите на https://www.mongodb.com/cloud/atlas
- Создайте бесплатный аккаунт и кластер
- Получите строку подключения

### 2. Настроить Backend

```bash
cd backend
npm install

# Создайте файл .env с содержимым:
# MONGODB_URI=mongodb://localhost:27017/wishlist
# (или ваша строка подключения от MongoDB Atlas)
# JWT_SECRET=your-secret-key-here
# PORT=5000
# NODE_ENV=development

npm run dev
```

### 3. Настроить Frontend

```bash
# В новом терминале
cd client
npm install
npm run dev
```

### 4. Открыть в браузере

Откройте адрес, который покажет Vite (обычно http://localhost:5173)

## Готово! 🎉

Теперь вы можете:
- Зарегистрироваться
- Войти
- Добавлять подарки с изображениями
- Редактировать и удалять подарки

## Если что-то не работает:

1. **Ошибка подключения к MongoDB:**
   - Проверьте, что MongoDB запущен (локально) или строка подключения правильная (Atlas)
   - Проверьте файл `.env` в папке `backend`

2. **Ошибка "Cannot find module":**
   - Убедитесь, что выполнили `npm install` в обеих папках (backend и client)

3. **Порт уже занят:**
   - Измените PORT в `.env` файле backend
   - Или измените порт Vite в `vite.config.js`

4. **Изображения не загружаются:**
   - Убедитесь, что папка `backend/public/uploads` существует (создается автоматически)
   - Проверьте, что сервер запущен на порту 5000

