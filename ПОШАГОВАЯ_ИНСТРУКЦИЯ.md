# Пошаговая инструкция по запуску MongoDB и приложения

## ✅ Шаг 1: Файл .env уже создан!

Файл `backend\.env` уже настроен с правильными параметрами.

## 🚀 Шаг 2: Запуск MongoDB

У вас есть **два способа** запустить MongoDB:

### Способ А: Запуск вручную (для тестирования)

1. **Дважды кликните на файл `START_MONGODB.bat`** в корне проекта
2. Оставьте это окно открытым - MongoDB будет работать
3. Вы увидите сообщения о запуске MongoDB

**Преимущества:** Просто, не требует прав администратора  
**Недостатки:** Нужно запускать каждый раз вручную

### Способ Б: Установка как службы Windows (рекомендуется)

1. **Правой кнопкой** на файл `START_MONGODB_SERVICE.bat`
2. Выберите **"Запуск от имени администратора"**
3. Это нужно сделать **ОДИН РАЗ**
4. После установки службы, запустите её:

```powershell
# Откройте PowerShell от имени администратора и выполните:
net start MongoDB
```

Или через графический интерфейс:
- Нажмите `Win + R`
- Введите `services.msc`
- Найдите "MongoDB"
- Правый клик → "Запустить"

**Преимущества:** Автоматический запуск при загрузке Windows  
**Недостатки:** Требует прав администратора для установки

## ✅ Шаг 3: Проверка работы MongoDB

После запуска MongoDB, откройте **новое** окно терминала и выполните:

```powershell
cd "C:\Program Files\MongoDB\Server\8.2\bin"
.\mongo.exe
```

Если видите приглашение `>`, значит MongoDB работает!  
Введите `exit` для выхода.

## 🎯 Шаг 4: Запуск Backend сервера

Откройте новый терминал:

```powershell
cd C:\Users\rudzi\Documents\wishlist\backend
npm install
npm run dev
```

Вы должны увидеть:
```
✅ MongoDB подключен
🚀 Сервер запущен на порту 5000
```

Если видите эти сообщения - всё работает! 🎉

## 🎨 Шаг 5: Запуск Frontend

Откройте **ещё один** новый терминал:

```powershell
cd C:\Users\rudzi\Documents\wishlist\client
npm install
npm run dev
```

Откройте браузер на адресе, который покажет Vite (обычно http://localhost:5173)

## 📝 Что делать дальше?

1. Зарегистрируйте новый аккаунт
2. Войдите в систему
3. Добавляйте подарки!

## ❌ Если что-то не работает:

### Ошибка "MongoDB подключен" не появляется:
- Проверьте, что MongoDB запущен (окно с mongod.exe открыто или служба запущена)
- Проверьте файл `backend\.env` - там должна быть строка `MONGODB_URI=mongodb://localhost:27017/wishlist`

### Ошибка "Cannot connect to MongoDB":
- Убедитесь, что MongoDB запущен
- Проверьте, что порт 27017 не занят другим приложением
- Попробуйте перезапустить MongoDB

### Ошибка при установке зависимостей:
- Убедитесь, что Node.js установлен: `node --version`
- Если нет Node.js, скачайте с https://nodejs.org/

## 💡 Совет

Для удобства создайте ярлыки на рабочий стол:
- `START_MONGODB.bat` - для запуска MongoDB
- Ярлык на папку проекта для быстрого доступа

